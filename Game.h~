#pragma once
// #include <SFML/Graphics.hpp>
#include "s_Window.h"
#include "World.h"
#include "snake.h"

// using namespace std;
// using namespace sf;

class Game{
  // this is to avoid the ambiguity 
  s_Window m_Window;
  sf::Clock m_clock;
  //NOTE: The actual time since the last frame(it represents for pc power/speed)
  float m_elapsed;

  World m_World;
  Snake m_Snake;
public:
  Game();
  ~Game();

  void Update();
  void Render();

  sf::Time GetElapsed();
  void RestartClock();

  s_Window*GetWindow();

  void HandleInput();
};
