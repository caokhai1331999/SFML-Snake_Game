#pragma once
#include <SFML/Graphics.hpp>
// May be the fault lie here we use the sfml time not
// std time
// #include <ctime>
// #include <cstdlib>
#include "snake.h"

using namespace sf;
using namespace std;

class World{
public:
  
  World(sf::Vector2u l_Window);
  ~World();
  int  GetBlockSize();
  void RespawnApple();

  void Update(Snake& l_player);
  void Render(sf::RenderWindow& l_Window);
private:
  sf::Vector2u m_WindowSize;
  sf::Vector2i m_item;

  int m_BlockSize;		// blocksize is size of one block
  sf::CircleShape m_appleShape;
  sf::RectangleShape m_bounds[4];
};
